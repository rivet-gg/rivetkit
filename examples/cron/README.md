---
title: "Cron"
description: "Schedule recurring commands with agentOS."
category: "Runtime"
order: 4
---

Schedule bounded command executions with cron expressions, inspect jobs,
subscribe to `cron.fired` events, and cancel jobs. Cron runs commands only.
