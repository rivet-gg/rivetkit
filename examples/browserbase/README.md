# Browserbase CLI in agentOS

This example installs the Browserbase `browse` CLI as ordinary VM software and
executes `browse cloud fetch` through the process API. Set
`AGENTOS_BROWSERBASE_PACKAGE_URL` to an immutable `.aospkg` URL and optionally
set `AGENTOS_BROWSERBASE_PACKAGE_DIGEST` before running it.
