# Browserbase CLI in agentOS

This example installs the Browserbase `browse` CLI as ordinary VM software and
executes `browse cloud fetch` through the process API. It does not include an
agent, an agent session, ACP, or a host filesystem mount.
